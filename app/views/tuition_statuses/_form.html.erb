<%= form_for(@tuition_status) do |f| %>
  <% if @tuition_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tuition_status.errors.count, "error") %> prohibited this tuition_status from being saved:</h2>

      <ul>
      <% @tuition_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br>
    <%= f.text_field :id %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
